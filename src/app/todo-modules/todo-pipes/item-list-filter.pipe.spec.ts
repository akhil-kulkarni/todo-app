import { ItemListFilterPipe } from './item-list-filter.pipe';

describe('ItemListFilterPipe', () => {
	it('create an instance', () => {
		const pipe = new ItemListFilterPipe();
		expect(pipe).toBeTruthy();
	});
});
